const Conf = (() => {

    const Conf = {
        NUM_SLIDERS: 64, // The number of parameters in an encoding (dim_z)
        FACES_GRID_NUM_CELLS_COLS: 5,
        FACES_GRID_NUM_CELLS_ROWS: 5,
    }

    return Conf
})();